using System;
using UnityEngine;
using UnityEngine.UI;

public class score : MonoBehaviour
{/*
    float StartPos = 0f;
    float CurrentPos = 0;
    public Transform player;
    public Text scoreText;

    void Start()
    {
        StartPos = player.position.z;
    }
    // Update is called once per frame
    void Update()
   {
        
        CurrentPos = player.position.z - StartPos;
        //Debug.Log(player.position.z - StartPos);
        scoreText.text = CurrentPos.ToString("0");
    }
*/
}
